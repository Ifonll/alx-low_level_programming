#include <stdio.h>

float sum();

void main(){
    float c;
    c = sum(7, 8);
    printf("%f\n", c);
}

float sum(){
    float a, b, sum =0;
    scanf("%f %f", &a, &b);
    sum = a + b;
    return 1.6, 1.8;
    return (sum, b);

}